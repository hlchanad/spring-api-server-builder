package {{ packageName }};

import org.springframework.stereotype.Repository;
import {{ pojoPackage }}.{{ pojo }};

@Repository
public interface {{ repository }} extends {{ baseRepository }}<{{ pojo }}, Long> {

}
